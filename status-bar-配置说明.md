# 福泽堂 MVU 前端状态栏 · 配置说明

## 一、工作原理

AI 每次回复末尾会输出 `<StatusPlaceHolderImpl/>`（由你世界书中的「变量输出格式强调」条目控制），酒馆正则脚本会匹配到这个标签，并将其**替换为状态栏 HTML 前端界面**。

```
AI回复正文...

<StatusPlaceHolderImpl/>        ← 酒馆正则匹配到这里
<UpdateVariable>                ← MVU变量更新（不被正则影响）
...
</UpdateVariable>
```

替换后，`<StatusPlaceHolderImpl/>` 的位置会渲染出状态栏 iframe。

---

## 二、酒馆正则配置

在 SillyTavern 的「正则表达式编辑器」中创建一条正则，按以下设置：

| 字段 | 值 |
|------|------|
| **脚本名称** | `状态栏`（或任意名称） |
| **查找正则表达式** | `<StatusPlaceHolderImpl/>` |
| **替换为** | 将 `status-bar.html` 的**完整 HTML 代码**粘贴到此处 |
| **修剪** | 留空 |

### 作用范围（左侧复选框）

| 选项 | 状态 |
|------|------|
| 用户输入 | ☐ 不勾 |
| **AI输出** | **☑ 必须勾选** |
| 快捷命令 | ☐ 不勾 |
| 世界信息 | ☐ 不勾 |
| 推理 | ☐ 不勾 |

### 其他选项（右侧复选框）

| 选项 | 状态 | 说明 |
|------|------|------|
| **已禁用** | **☐ 不勾** | 勾了就不会工作！配置完成后确保取消勾选 |
| **在编辑时运行** | **☑ 勾选** | 编辑消息后也能重新渲染状态栏 |
| **仅格式显示** | **☑ 勾选** | 只影响显示，不会污染发给 AI 的提示词 |
| 仅格式提示词 | ☐ 不勾 | — |

### 其他设置

| 字段 | 值 |
|------|------|
| 正则表达式查找时的宏 | `不替换` |
| 最小深度 | `无限` |
| 最大深度 | `无限` |

完成后点击「保存」。

---

## 三、前提条件：世界书 MVU 条目

你的世界书中以下条目必须保持启用状态（你目前已经全部配置好了）：

| 条目 | 条目名 | 状态 |
|------|--------|------|
| 变量初始化 | `[initvar]变量初始化勿开` | ⛔ **关闭**（仅首次初始化时临时开启） |
| 变量列表 | `变量列表` | ✅ 启用 |
| 变量更新规则 | `[mvu_update]变量更新规则` | ✅ 启用 |
| 变量输出格式 | `[mvu_update]变量输出格式` | ✅ 启用 |
| 变量输出格式强调 | `[mvu_update]变量输出格式强调` | ✅ 启用 |

特别注意：「变量输出格式强调」中已经包含了 `<StatusPlaceHolderImpl/>`，这是正则匹配的锚点：

```yaml
变量输出格式强调:
  rule:
    - The following must be inserted to the end of reply, and cannot be omitted.
    - DO NOT wrap it with markdown code blocks (like ```xml). Output raw text directly.
  format: |-
    <StatusPlaceHolderImpl/>
    <UpdateVariable>
    ...
    </UpdateVariable>
```

---

## 四、状态栏展示的变量

全部来自 MVU 的 `stat_data`，对应你的初始变量结构：

```
stat_data
├── 世界状态
│   ├── 今日黄历        → 顶部黄历金色栏
│   ├── 当前时间        → "时辰"
│   ├── 当前地点        → "所在"
│   ├── 暗骰点数        → "暗骰" (D100 → X)
│   └── 在场人物        → 标签列表
├── 福泽堂
│   ├── 经营时间        → "苟活第 X 天"
│   ├── 完成单数        → "完成单数"
│   └── 库房            → 物品标签（鼠标悬停看描述）
├── 袁枚
│   └── 七宝收集进度    → 进度条 (0~6)
└── 主线
    └── 是否开启        → 主线状态提示
```

---

## 五、常见问题

### Q: 状态栏没有显示？

1. 正则脚本的「已禁用」是否**取消勾选**了？
2. AI 回复末尾是否输出了 `<StatusPlaceHolderImpl/>`？如果没有，检查「变量输出格式强调」条目是否启用
3. MVU 变量框架脚本是否正常运行？（状态栏依赖 `waitGlobalInitialized('Mvu')`）

### Q: 变量数据没有更新？

代码中已经监听了 `Mvu.events.VARIABLE_UPDATE_ENDED` 事件，变量更新后会自动刷新。如果没有：
1. 检查 AI 回复末尾是否输出了 `<UpdateVariable>` 块
2. 检查「变量更新规则」和「变量输出格式」条目是否启用

### Q: 想让状态栏默认展开？

修改 HTML 中两处：
1. 将 `.fzt-body` 的 CSS 中 `display: none` 改为 `display: block`
2. 将 HTML 中 `[ 点击展开 ]` 改为 `[ 点击收起 ]`
