# 福泽堂 MVU 前端状态栏 · 配置说明

## 一、工作原理

AI 每次回复末尾会输出 `<StatusPlaceHolderImpl/>`（由你世界书中的「变量输出格式强调」条目控制），酒馆正则脚本会匹配到这个标签，并将其**替换为状态栏 HTML 前端界面**。

```
AI回复正文...

<StatusPlaceHolderImpl/>        ← 酒馆正则匹配到这里
<UpdateVariable>                ← MVU变量更新（不被正则影响）
...
</UpdateVariable>
```

替换后，`<StatusPlaceHolderImpl/>` 的位置会渲染出状态栏 iframe。

---

## 二、酒馆正则配置（共 2 条正则）

---

### 正则 1：状态栏

将 `<StatusPlaceHolderImpl/>` 在显示时替换为状态栏界面。

| 字段 | 值 |
|------|------|
| **脚本名称** | `状态栏` |
| **查找正则表达式** | `<StatusPlaceHolderImpl/>` |
| **替换为** | 将 `status-bar.html` 的**完整 HTML 代码**粘贴到此处 |

| 作用范围 | 状态 | 其他选项 | 状态 |
|----------|------|----------|------|
| 用户输入 | ☐ | 已禁用 | ☐ |
| **AI输出** | **☑** | **在编辑时运行** | **☑** |
| 快捷命令 | ☐ | **仅格式显示** | **☑** |
| 世界信息 | ☐ | 仅格式提示词 | ☐ |
| 推理 | ☐ | | |

---

### 正则 2：隐藏变量更新

将 `<UpdateVariable>...</UpdateVariable>` 在显示时隐藏。

| 字段 | 值 |
|------|------|
| **脚本名称** | `隐藏变量更新` |
| **查找正则表达式** | `<UpdateVariable>[\s\S]*?</UpdateVariable>` |
| **替换为** | **留空**（什么都不填） |

| 作用范围 | 状态 | 其他选项 | 状态 |
|----------|------|----------|------|
| 用户输入 | ☐ | 已禁用 | ☐ |
| **AI输出** | **☑** | **在编辑时运行** | **☑** |
| 快捷命令 | ☐ | **仅格式显示** | **☑** |
| 世界信息 | ☐ | 仅格式提示词 | ☐ |
| 推理 | ☐ | | |

> **重要**：两条正则都勾「**仅格式显示**」，只影响页面展示，不影响发给 AI 的提示词和 MVU 框架。

---

### 通用设置（两条正则相同）

| 字段 | 值 |
|------|------|
| 正则表达式查找时的宏 | `不替换` |
| 最小深度 | `无限` |
| 最大深度 | `无限` |

### 总结

| 正则 | 匹配 | 替换为 | 仅格式显示 | 仅格式提示词 |
|------|------|--------|------------|--------------|
| `状态栏` | `<StatusPlaceHolderImpl/>` | HTML代码 | ☑ | ☐ |
| `隐藏变量更新` | `<UpdateVariable>[\s\S]*?</UpdateVariable>` | 空 | ☑ | ☐ |

---

## 三、变量结构脚本（Zod Schema）—— 必需！

**这是 MVU 框架的核心组件**。没有它，MVU 无法解析变量，状态栏无法获取数据。

### 配置方法

将 `mvu-schema.js` 的内容添加为**角色脚本**（不是世界书条目）：

1. 打开 SillyTavern → 角色卡编辑 → 脚本（Scripts）
2. 新建一个脚本，名称如 `MVU变量结构`
3. 将 `mvu-schema.js` 的完整代码粘贴进去
4. 保存

> 注意：变量结构脚本必须放在**角色脚本**中，不能放在世界书里。

---

## 四、前提条件：世界书 MVU 条目

你的世界书中以下条目必须正确配置：

| 条目 | 条目名 | 启用状态 | 位置 | 深度 | 顺序 |
|------|--------|----------|------|------|------|
| 变量初始化 | `[initvar]变量初始化勿开` | ⛔ **关闭** | D齿轮在深度 | 4 | 200 |
| 变量列表 | `变量列表` | ✅ 启用 | D齿轮在深度 | 0 | 200 |
| 变量更新规则 | `[mvu_update]变量更新规则` | ✅ 启用 | D齿轮在深度 | 0 | 200 |
| 变量输出格式 | `[mvu_update]变量输出格式` | ✅ 启用 | D齿轮在深度 | 0 | 200 |
| 变量输出格式强调 | `[mvu_update]变量输出格式强调` | ✅ 启用 | D齿轮在深度 | 0 | 200 |

### ⚠️ 你的世界书中 `[initvar]变量初始化勿开` 配置有误：

| 属性 | 当前值（有误） | 应该改为 |
|------|---------------|----------|
| 位置 | before_char (0) | **D齿轮在深度 (4)** |
| 顺序 | 100 | **200** |

请在 SillyTavern 中修改该条目的位置和顺序。

---

### 关于 `<StatusPlaceHolderImpl/>`

「变量输出格式强调」条目中已包含 `<StatusPlaceHolderImpl/>`：

```yaml
变量输出格式强调:
  rule:
    - The following must be inserted to the end of reply, and cannot be omitted.
    - DO NOT wrap it with markdown code blocks (like ```xml). Output raw text directly.
  format: |-
    <StatusPlaceHolderImpl/>
    <UpdateVariable>
    ...
    </UpdateVariable>
```

---

## 五、状态栏展示的变量

全部来自 MVU 的 `stat_data`，对应你的初始变量结构：

```
stat_data
├── 世界状态
│   ├── 今日黄历        → 顶部黄历金色栏
│   ├── 当前时间        → "时辰"
│   ├── 当前地点        → "所在"
│   ├── 暗骰点数        → "暗骰" (D100 → X)
│   └── 在场人物        → 标签列表
├── 福泽堂
│   ├── 经营时间        → "苟活第 X 天"
│   ├── 完成单数        → "完成单数"
│   └── 库房            → 物品标签（鼠标悬停看描述）
├── 袁枚
│   └── 七宝收集进度    → 进度条 (0~6)
└── 主线
    └── 是否开启        → 主线状态提示
```

---

## 六、常见问题

### Q: 状态栏显示了但变量是初始值 / 没有更新？

最常见原因：**缺少 Zod Schema 变量结构脚本**。请检查：
1. 是否已将 `mvu-schema.js` 添加为角色脚本？（见第三节）
2. 角色脚本是否启用？

其他检查：
3. AI 回复末尾是否输出了 `<UpdateVariable>` 块？
4. 「变量更新规则」和「变量输出格式」条目是否启用？
5. `[initvar]变量初始化勿开` 的位置是否设为「D齿轮在深度」、深度 4、顺序 200？

### Q: 状态栏没有显示？

1. 正则脚本的「已禁用」是否**取消勾选**了？
2. AI 回复末尾是否输出了 `<StatusPlaceHolderImpl/>`？如果没有，检查「变量输出格式强调」条目是否启用
3. MVU 变量框架脚本是否正常运行？（状态栏依赖 `waitGlobalInitialized('Mvu')`）
